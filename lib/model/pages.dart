import 'package:flutter/material.dart';

class Page {
  final String title;
  final Widget widget;
  final int index;
  final IconData iconData;

  Page(this.title, this.widget, this.index, this.iconData);

}